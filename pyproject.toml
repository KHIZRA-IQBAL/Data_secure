[project]
name = "secure-data"
version = "0.1.0"
description = "A secure data handling application"
readme = "README.md"
requires-python = ">=3.11"

[tool.poetry.dependencies]
python = "^3.11"  
streamlit = "^1.0.0"
cryptography = "^3.0"

